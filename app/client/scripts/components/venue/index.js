import React from 'react';
import Map from '../map';

let Venue = React.createClass({

    render() {
        return (
            <div>
                <Map />
                Venue
            </div>
        );
    }
});

export default Venue;
